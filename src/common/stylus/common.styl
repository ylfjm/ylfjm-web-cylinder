.table_content {
    width: 100%;
    //box-shadow: 0 2px 12px 0 rgba(0, 0, 0, .1);
    border-radius: 4px;
    margin-top: 10px;
}

.el-table {
    border-radius: 4px 4px 0 0;
    box-shadow: 0 2px 12px 0 rgba(0, 0, 0, .1);
}

.el-table td {
    border-bottom: 0;
}

.pagination_box
    height 40px
    position: relative
    background-color #fff
    box-shadow: 0 2px 12px 0 rgba(0, 0, 0, .1);

    .pagination_content
        position absolute
        right 28px
        top 4px